import Config

config :json_ld,
  # comment this in to run tests against the test files hosted on GitHub (takes much longer and fails occasionally)
  # w3c_test_suite_run_mode: :remote,
  max_contexts_loaded: 5
